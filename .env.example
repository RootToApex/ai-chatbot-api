# 로컬 개발용 DB 접속 정보 — 값을 채워 .env로 복사해서 쓴다 (cp .env.example .env)
# 값을 안 채워도 compose.yml·application.yml의 :-기본값(assignment)으로 로컬 실행은 된다
DB_HOST=
DB_PORT=
DB_NAME=
DB_USER=
DB_PASSWORD=
# AI 키가 필요한 경우에만 (기본 실행은 키 없이 가능해야 함)
# AI_API_KEY=
